@namespace(uri="http://genericity/examples/graph_paths", prefix="GraphPaths")
package GraphPaths;

class PathModel {
  val Node[*] nodes;
}

class Node {
  attr String label;
  val  Path[*] paths;
}

class Path {
   ref Node[1] target;
   attr Integer[1] cost;
   ref Node[*] hops;
}